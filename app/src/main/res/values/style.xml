<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/corPrincipal</item>
        <item name="colorPrimaryDark">@color/corPrincipalEscura</item>
        <item name="colorAccent">@color/corPrincipalClara</item>

        <!-- Add this -->

        <item name="android:dialogTheme">@style/DialogStyle</item>
        <item name="android:alertDialogTheme">@style/DialogStyle</item>

    </style>

    <!-- Personalize a cor aqui -->

    <style name="DialogStyle" parent="android:Theme.Material.Dialog">
        <item name="android:colorAccent">@color/corPrincipalEscura</item>
    </style>



</resources>